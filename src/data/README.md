# Demo data

The browser-only Vite mode uses deterministic fixtures from this folder. They
exercise the same public TypeScript contracts as Tauri without claiming to be
observed local execution. Desktop mode never substitutes these fixtures for a
backend result.
