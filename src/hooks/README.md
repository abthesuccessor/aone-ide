# React hooks

Reusable UI behavior lives here when it is independent of one screen. Hooks must
clean up timers, listeners, and animation frames, remain safe under React Strict
Mode, and avoid performing privileged operations directly.
