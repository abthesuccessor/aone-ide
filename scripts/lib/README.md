# Release libraries

These dependency-light ES modules implement release confinement, manifests,
proofs, checksums, and Apple artifact verification. Entry scripts compose them;
security-sensitive helpers stay independently testable and never print secret
values.
